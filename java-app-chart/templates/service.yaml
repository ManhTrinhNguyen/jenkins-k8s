apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName  }}
  labels:
    app: {{ .Values.appName }}
spec:
  type: {{ .Values.serviceType }}
  ports:
    - port: {{ .Values.svcPort  }}
      targetPort: {{ .Values.appPort  }}
      protocol: TCP
  selector:
    app: {{ .Values.appName }}
